import * as React from 'react';
import * as ReactDOM from 'react-dom';
import {HashRouter as Router, Route, Link} from 'react-router-dom';
import { readSync } from 'fs';

ReactDOM.render(
  <div>React-zeit-ui</div>,
document.querySelector('#root'));
